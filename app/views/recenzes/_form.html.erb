<%= form_with(model: recenze) do |form| %>
<div className="one-movie">
  <% if recenze.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recenze.errors.count, "error") %> prohibited this recenze from being saved:</h2>

      <ul>
        <% recenze.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :hodnoceni, style: "display: block" %>
    <%= form.text_field :hodnoceni %>
  </div>
    

  <div>
    <%= form.submit %>
  </div>
  </div>
<% end %>
